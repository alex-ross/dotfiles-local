set-option -g default-command "reattach-to-user-namespace -l zsh"
set-option -sg escape-time 0

# Set color for active window in status bar
set-window-option -g window-status-current-bg colour24 # blue
